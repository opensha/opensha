CORE NOTES:
* deleted all GEM

CORE TODO:

* remove GEM
* resolve path forward for ERF/AttenRel Ref objects? current AttenRelRel in core, ERF_Ref in apps
* restore any nshmp[2] needed
	* including IMRs in AttenRelRef that point to nshmp2
* move FaultSystemSolutionERF to core. TRICKY
* resurrect simulator UCERF3 section ID iden
* mag freq dist app is in core because the MFD parameter uses the actual app, not just editors. Now launcher in apps, window in core
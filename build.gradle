/*
 * Taks/definitions specific to opensha-ucerf3
 */

apply from: '../opensha-commons/build-common.gradle'

dependencies {
	/*
	 *	"Runtime" dependencies are exported downstream to users
	 */
	// repository based dependencies
	 
    // jars. TODO move to repo?
    
    compileOnly project(path: ':opensha-core', configuration: 'compileOnly')
    compileAndResource project(path: ':opensha-core')

    // Use JUnit test framework
    testCompile 'junit:junit:4.12'
}
